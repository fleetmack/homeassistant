alias: Blinds — Reset Manual Override at Midnight
description: ""
triggers:
  - at: "00:00:00"
    trigger: time
actions:
  - target:
      entity_id: input_boolean.blinds_manual_override
    action: input_boolean.turn_off
mode: single
